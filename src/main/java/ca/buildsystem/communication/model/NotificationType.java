package ca.buildsystem.communication.model;

/**
 * Enum representing the type of notification.
 */
public enum NotificationType {
    EMAIL,
    SMS,
    IN_APP // For future use, potentially web push or similar
}

